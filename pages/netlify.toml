[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20.0.0"

# SPA routing (React Router) - يرجع كل المسارات إلى index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
